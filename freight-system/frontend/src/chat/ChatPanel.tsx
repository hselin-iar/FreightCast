import React from 'react';
/** ChatPanel — chat UI, tool-calling round trip, receives dashboard_update events.
 *  Stub, populated in Build Step 13. */
const ChatPanel: React.FC = () => <></>;
export default ChatPanel;
